# CLAUDE.md

See @AGENTS.md for all project rules and conventions.

Additional Claude Code specific rules are in `.claude/rules/`.

## Quick Reference

- Test-first development: write failing tests before implementation
- Conventional commits required: `🔴 test()`, `🟢 feat()`, `♻️ refactor()`, `🚀 deploy()`
- Check `docs/adr/records/` before making architectural decisions
- Check `.agentile/skills/` for task-specific guidance
- Run `.agentile/validate.mjs` before committing
